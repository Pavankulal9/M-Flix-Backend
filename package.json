{
  "name": "m-flix-api",
  "version": "1.0.0",
  "description": "Its an backend for m-flix-app. TMDB which used to fetch movie data is blacklisted by indian government for some reason so im create just an intermediate backend so that i can deploy this API in an american server and it work fine.",
  "main": "index.js",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsx --watch ./src/index.ts",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pavan",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "date-fns": "^4.1.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "helmet": "^8.0.0",
    "tsup":  "^8.3.5",
    "tsx":  "^4.19.2",
    "http-status-codes": "^2.3.0",
    "winston": "^3.16.0"
  },
  "devDependencies": {
    "@types/compression": "^1.7.5",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.9.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.13.0"
  }
}
