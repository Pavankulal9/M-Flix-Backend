export enum ErrorStatus {
  Fail = 'fail', // Indicates an operation failure due to logical errors or invalid inputs.
  Error = 'error' // Represents a system error, such as an exception or an unexpected problem.
}